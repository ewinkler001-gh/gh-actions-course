name: 02 - Workflow Events

on: push

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: echo event
        run: echo "Event name:"
        # run: echo "Event name: ${{ env.GITHUB_EVENT_NAME }}"
